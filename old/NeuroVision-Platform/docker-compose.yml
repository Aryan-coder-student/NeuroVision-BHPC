version: '3.8'

services:
  mailhog:
    image: mailhog/mailhog
    container_name: neurovision_mailhog
    ports:
      - "1025:1025" # SMTP port
      - "8025:8025" # Web UI port
    restart: always
